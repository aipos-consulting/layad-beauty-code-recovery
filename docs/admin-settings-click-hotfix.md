# Admin settings click hotfix

The operational data screen still renders a legacy `/admin` href for the Operation Settings menu. A capture-phase click guard prevents that legacy navigation and routes directly to `/admin/settings`.
